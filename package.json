{
  "private": true,
  "name": "mdx-desk-to-now",
  "version": "1.0.0",
  "scripts": {
    "start": "mdx-deck deck.mdx",
    "build": "mdx-deck build deck.mdx",
    "help": "mdx-deck",
    "now-build": "mdx-deck build deck.mdx --no-html && cp -r images dist/ && cp -r snippets dist/"
  },
  "devDependencies": {
    "mdx-deck": "^2.0.0"
  }
}
